server:
  port: 20300

spring:
  application:
    name: life-user
  datasource:
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3306/life?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver
mybatis:
  mapper-locations:
    - classpath:mapper/*.xml

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:20000/eureka/